Django==5.1.3
psycopg2-binary==2.9.9 #driver do PostgreSQL, sem ele o django não fala com o Supabase
djangorestframework==3.15.2 #necessário para criar a API REST
python-dotenv==1.0.1 #necessário para ler as variáveis de ambiente do arquivo .env